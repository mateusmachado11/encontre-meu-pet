import React from 'react';

const AdBanner = ({ id }) => (
    <div className="bg-gray-200 text-gray-500 text-center py-10 rounded-lg my-4">
        Espaço Publicitário - {id}
    </div>
);

export default AdBanner;
